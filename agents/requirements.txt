# Agent runtime dependencies.
#
# The mcp upper bound is load-bearing: mcp 2.x moved `mcp.shared.session`, and
# google-adk imports it. Without the bound a fresh install fails to import.
google-adk>=0.5.0,<3.0.0
mcp>=1.0.0,<2.0.0
python-dotenv>=1.0.0
pydantic>=2.0.0
requests>=2.31.0
pymongo>=4.17.0
google-auth>=2.28.0

# Test only
pytest>=8.0.0
